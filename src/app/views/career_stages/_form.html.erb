<%= form_with(model: career_stage, local: true) do |form| %>
  <% if career_stage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(career_stage.errors.count, "error") %> prohibited this career_stage from being saved:</h2>

      <ul>
      <% career_stage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <table width="100%">
    <tr>
      <td>Name</td>
      <td><%= form.text_field :name, {:class => 'form-control'} %></td>
    </tr>
 </table>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
