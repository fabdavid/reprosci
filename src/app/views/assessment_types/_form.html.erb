<%= form_with(model: assessment_type, local: true) do |form| %>
  <% if assessment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assessment_type.errors.count, "error") %> prohibited this assessment_type from being saved:</h2>

      <ul>
      <% assessment_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table width="100%">
    <tr>
      <td>Name</td>
      <td><%= form.text_field :name, {:class => 'form-control'} %></td>
    </tr>
    <tr>
      <td>Description</td>
      <td><%= form.text_area :description, {:class => 'form-control'} %></td>
    </tr>

    <tr>
      <td>Icon classes</td>
      <td><%= form.text_field :icon_classes, {:class => 'form-control'} %></td>
    </tr>

    

  </table>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
