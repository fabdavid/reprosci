<%= form_with(model: assertion_type, local: true) do |form| %>
  <% if assertion_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assertion_type.errors.count, "error") %> prohibited this assertion_type from being saved:</h2>

      <ul>
      <% assertion_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
