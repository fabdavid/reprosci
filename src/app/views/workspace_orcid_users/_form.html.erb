<%= form_with(model: workspace_orcid_user, local: true) do |form| %>
  <% if workspace_orcid_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workspace_orcid_user.errors.count, "error") %> prohibited this workspace_orcid_user from being saved:</h2>

      <ul>
      <% workspace_orcid_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
