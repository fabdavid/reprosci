<p id="notice"><%= notice %></p>

<h1>Rels</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rels.each do |rel| %>
      <tr>
        <td><%= link_to 'Show', rel %></td>
        <td><%= link_to 'Edit', edit_rel_path(rel) %></td>
        <td><%= link_to 'Destroy', rel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rel', new_rel_path %>
