<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN"
  "http://www.w3.org/TR/html4/loose.dtd">
<html lang="en">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <style type="text/css" media="screen" class = "content">
    <%# stylesheet_link_tag "bootstrap.min.css" %>

      h3 { color: #f00; }
      ul { list-style: none; }
    </style>
  </head>
  <body>
    <strong><p class="text-warning"><%= @user.email %> wants to share this workspace with you: "<%= @workspace.name %>"</p></strong>
    <%# existing_user = User.where(:email => @share_user.email).first %>
    <% if !@share_user %>
      <p>As you are not yet a user of ReproSci, please first create your account following this <a href='<%= APP_CONFIG[:server_url] + new_user_registration_path %>?user[email]=<%= @share.email %>'>link</a></p>
    <% end %>
    <p>After signin you will be able to 
      <%# if @share_user %> <%# u = User.where(:email => @share.email).first %>
      <a href='<%=  APP_CONFIG[:server_url] + new_user_session_path %>?user[email]=<%= @share.email %>'>login</a> and access the workspace <a href="<%=  APP_CONFIG[:server_url] + workspace_path(@workspace.key) %>">here</a>
<!--      <%# else %>
       <a href='<%=  APP_CONFIG[:server_url] + new_user_registration_path %>?user[email]=<%= @share.email %>'>signin</a> and access the workspace <a href="<%=  APP_CONFIG[:server_url] + workspace_path(@workspace.key) %>">here</a> -->
      <%# end %>
    </p>
    
<% if @password %>
Temporary password: <%= @password %>
<% end %> 
    

    The ReproSci team --
  </body>
</html>
