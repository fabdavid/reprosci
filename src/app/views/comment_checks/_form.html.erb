<%= form_with(model: comment_check, local: true) do |form| %>
  <% if comment_check.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_check.errors.count, "error") %> prohibited this comment_check from being saved:</h2>

      <ul>
      <% comment_check.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
